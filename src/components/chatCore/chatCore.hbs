<div>
  {{#if selectedChat }}  
    <div class="chat_messages">
       {{#each messages}}
        {{{this}}}
      {{/each}}
    </div>
    <form class="chat_input-block">
      <button class="chat_attach-button"></button>
      {{{ input }}}
      <label class="error">{{error}}</label>
      {{{ button }}}
    </form>
  {{/if}}
</div>
